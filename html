<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Для тебя ❤️</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: #f3e5f5; /* Нежно-фиолетовый фон */
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
            color: #6a1b9a; /* Тёмно-фиолетовый текст */
            text-align: center;
            cursor: pointer;
        }
        
        .photo {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            object-fit: cover;
            border: 5px solid #4a148c; /* Тёмно-фиолетовая рамка */
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            margin-bottom: 30px;
            transition: transform 0.3s;
        }
        
        .photo:hover {
            transform: scale(1.05); /* Эффект увеличения при наведении */
        }
        
        .text {
            font-size: 24px;
            max-width: 600px;
            opacity: 0;
            white-space: pre-line;
            line-height: 1.5;
            margin: 0 20px;
        }

        /* Анимация сердечек */
        .heart {
            position: absolute;
            pointer-events: none;
            animation: float 4s ease-in-out infinite;
        }

        @keyframes float {
            0% { transform: translateY(0) rotate(0deg); opacity: 1; }
            100% { transform: translateY(-100px) rotate(360deg); opacity: 0; }
        }
    </style>
</head>
<body>
    <img src="photo.jpg" alt="Твоя фотография" class="photo">
    <div class="text" id="typed-text"></div>

    <script>
        // Твой текст
        const text = `Каждый раз, когда я скучаю по тебе,\nя смотрю на твоё милое фото,\nи когда я смотрю — влюбляюсь в тебя\nещё больше, чем раньше.\n\nЖаным, ты у меня самая красивая,\nи моя любовь к тебе растёт так же,\nкак понимание, что ты —\nсамая важная часть моей жизни. ❤️`;
        const element = document.getElementById("typed-text");
        let i = 0;

        function typeWriter() {
            if (i < text.length) {
                element.innerHTML += text.charAt(i);
                i++;
                setTimeout(typeWriter, 50);
            } else {
                element.style.opacity = 1;
            }
        }

        // Анимация сердечек при клике
        document.body.addEventListener('click', function(e) {
            const heart = document.createElement('div');
            heart.innerHTML = '❤️';
            heart.className = 'heart';
            heart.style.left = e.clientX + 'px';
            heart.style.top = e.clientY + 'px';
            document.body.appendChild(heart);
            
            setTimeout(() => {
                heart.remove();
            }, 4000);
        });

        // Запуск анимации текста
        window.onload = function() {
            setTimeout(typeWriter, 1000);
        };
    </script>
</body>
</html>
